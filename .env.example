# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com
VITE_PUBLIC_SUPABASE_URL=your_supabase_project_url
VITE_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# EmailJS is handled entirely server-side now (booking-emails edge function).
# Keys/template ids live in Supabase edge function secrets + private.app_secrets —
# no VITE_ EmailJS variables should ever be added here (they end up in the public JS bundle).

# Test Configuration (for scripts/book-test-reminder.mjs)
TEST_REMINDER_EMAIL=test@example.com

# Restaurant Configuration
# Optional: Defaults to "Atlantic/Canary" if not set
RESTAURANT_TIMEZONE=Atlantic/Canary
